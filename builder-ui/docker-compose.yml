version: "3.5"
services:
  script:
    build: .
    environment:
    - OPERATION=checker
    - REPO=https://github.com/dappnode/welcome
    - COMMIT=c83f85de57a5c28103364b55c1baf59867cf9b66
    - ENS=ENS_VALUE
    - PRIVATE_KEY=private_key_Value
    image: "check-tool"
    restart: on-failure